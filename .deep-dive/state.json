{
  "lastRunCommit": "08cb9d1ece81b27737037e5e84f8a2a16c47f622",
  "lastRunTimestamp": "2026-09-08T18:10:00Z",
  "mode": "debt",
  "filesAnalyzed": 8,
  "findingsCount": 2,
  "coverageGaps": [
    "The README corpus figures were RE-MEASURED this pass and are reproducible only as a dated snapshot: the archive is live (sessions are written while a sweep runs) and transcripts rotate away, so it shrank from 629 to 611 top-level sessions since publication. The classifier's own effect is pinned separately against a frozen 135,618-command set, which IS reproducible.",
    "The GitHub Actions workflow was not executed. The uv sync --all-extras --frozen change is verified by running that exact command locally and then the suite and ruff from the resulting environment, not by observing a CI run.",
    "Failure-marker detection was validated against synthetic outputs and the corpus aggregate, not against a labelled ground-truth set of red sessions - no such labelling exists. The direction is safe by construction: markers can only turn a green red, never the reverse.",
    "stdio transport under a real MCP client is still not exercised; unchanged this pass.",
    "Two known misclassifications remain from heredoc and multi-line-quote blindness, measured at two commands in 135,618 and filed as open entry 1."
  ],
  "notes": "Debt pass: all six open entries closed, two new (lower-class) ones filed from evidence gathered while closing them. Unblocked by finding that the README corpus is the local ~/.claude/projects archive and still present, which made the two \"re-bases a published figure\" items measurable."
}
